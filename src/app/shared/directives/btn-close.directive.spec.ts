import { BtnCloseDirective } from './btn-close.directive';

describe('BtnCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnCloseDirective();
    expect(directive).toBeTruthy();
  });
});
